# Copy to .env and adjust as needed. All values are optional.

# Point the app at a data directory other than the repo root.
# Leave unset to use the bundled sample data in ./conferences and ./talks.
# CONFETTI_DATA_DIR=/path/to/your/conference/data

# IANA timezone for Google Calendar events (default: Europe/Amsterdam).
# CONFETTI_TIMEZONE=Europe/Paris
